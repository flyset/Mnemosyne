# MyMCP 0.9.0: configurable protocol-header strictness

Schema 6 adds `[mcp].strict_protocol_version`. `true` preserves strict handling;
`false` permits only an absent protocol header on an authenticated, validated
registered session. MCP revision remains `2025-11-25`; supplied invalid headers
and anonymous behavior remain strict.
